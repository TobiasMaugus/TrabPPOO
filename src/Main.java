/**
 * Classe principal responsável por iniciar a aplicação.
 * Contém o método de entrada (main) que instancia e executa a simulação.
 */
public class Main{
  /**
   * Ponto de entrada do programa.
   * Cria uma nova instância do simulador para iniciar a execução.
   * @param args Argumentos da linha de comando (não utilizados nesta simulação).
   */
  public static void main(String[] args){
    new Simulator();
  }
}